
<div class="p-5 mb-4 bg-light rounded-3">
  <%= link_to "🔙", restaurants_path, class:'link-icon' %>
  <div class="container-fluid py-5">
    <div class="row">
      <div class="col-md-8">
        <h1 class="display-5 fw-bold"><%= @restaurant[:name] %></h1>
        <p class=""><%=  @restaurant[:address] %></p>
        <p class=""><%=  @restaurant[:phone_number] %></p>
        <p>Reviews :</p>
        <% @restaurant.reviews.each do |review|%>
        <p> <%= review.rating %></p>
        <p> <%= review.content %></p>
        <% end %>
        <%= link_to "Write new review", new_restaurant_review_path(@restaurant), class: 'm-3' %>
      </div>
      <div class="col-md-4">
        <div class="d-flex flex-column align-items-center justify-content-center">
          <%# <%= link_to "Edit the restaurant", edit_restaurant_path, class: 'm-3' %>
          <%= link_to "🗑",
                      restaurant_path,
                      method: :delete,
                      data: { confirm: "Are you sure?" },
                      class:'link-icon' %>
        </div>
      </div>
  </div>

    <%# <button class="btn btn-primary btn-lg" type="button">Example button</button> %>
  </div>
</div>
